modules:
  - name: rose-cream-pasta
    type: nodejs
    path: .
    command: yarn start:prod
    build-parameters:
      builder: custom
      commands:
        - yarn install --production=false
        - yarn build
